# 基于 Spring Boot 的仓库管理系统设计与实现

---

## 摘要

**摘要**：随着企业数字化转型的深入推进，传统的仓库管理方式存在数据孤岛、库存不清、手工记录效率低等问题。本文设计并实现了一款基于 Spring Boot 的仓库管理系统，采用 B/S 架构，集成 MyBatis Plus 作为 ORM 框架，MySQL 作为数据存储。系统涵盖用户认证、商品管理、入库管理、出库管理、库存预警及数据仪表盘等核心功能模块。在安全方面，采用 BCrypt 算法对用户密码加密存储，并通过 Token 实现接口层的身份校验。入库和出库操作基于 Spring 声明式事务管理，确保库存数据的一致性。系统前后端分离，后端提供 RESTful API 接口，便于后续扩展与维护。

**关键词**：仓库管理；Spring Boot；库存管理；BCrypt；MyBatis Plus

---

## 目录

（在 Word 中插入自动目录即可）

---

## 第一章 绪论

### 1.1 项目背景与意义

随着电子商务和物流行业的快速发展，企业对于仓库管理的精细化、信息化需求日益增长。传统的人工记账或电子表格管理方式已无法满足现代企业对库存实时性、准确性的要求。开发一套高效、稳定的仓库管理系统，能够帮助企业实现库存数据的集中管理，降低运营成本，提升管理效率。

### 1.2 国内外研究现状

目前，市场上主流的仓库管理系统大多功能庞大、部署复杂、价格昂贵。中小型企业往往难以承担其采购和维护成本。开源社区也存在部分仓库管理方案，但在易用性、安全性方面仍有较大提升空间。因此，开发一款轻量级、低成本且功能完备的仓库管理系统具有现实意义。

### 1.3 本文主要工作

本文围绕仓库管理系统的设计与实现展开，主要工作包括：

1. 分析仓库管理的核心业务流程，梳理功能需求
2. 设计系统数据库结构，包括商品、库存、入库单、出库单等核心数据表
3. 基于 Spring Boot 实现各功能模块，包括用户认证、商品管理、出入库操作及仪表盘统计
4. 对系统进行功能性测试，验证各模块的正确性

---

## 第二章 相关技术与开发环境

### 2.1 Spring Boot 框架

Spring Boot 是基于 Spring 框架的快速开发框架，通过自动配置和起步依赖简化了 Spring 应用的搭建过程。本系统基于 Spring Boot 3.x 版本开发，内置 Tomcat 容器，支持快速启动与部署。

### 2.2 MyBatis Plus

MyBatis Plus 是 MyBatis 的增强工具，在其基础上提供了内置的 CRUD 方法、分页插件、乐观锁支持等功能。本系统采用 MyBatis Plus 作为 ORM 框架，大幅减少了底层 SQL 的编写量。

### 2.3 MySQL 数据库

MySQL 是一款开源的关系型数据库管理系统，具有体积小、速度快、成本低等特点。本系统选用 MySQL 8.0 作为数据存储方案。

### 2.4 BCrypt 加密算法

BCrypt 是一种基于 Blowfish 的加密算法，其核心特性是内置盐值（Salt）和可调的加密强度，能够有效抵御彩虹表攻击。本系统采用 Spring Security Crypto 提供的 BCryptPasswordEncoder 实现密码加密。

### 2.5 开发与环境

- 开发语言：Java 17
- 开发工具：IntelliJ IDEA
- 数据库：MySQL 8.0
- 构建工具：Maven
- 接口测试工具：Apifox

---

## 第三章 系统需求分析

### 3.1 功能需求

本系统面向仓库管理人员，核心功能需求如下：

1. **用户管理**：用户注册与登录，Token 身份认证
2. **商品管理**：商品的增删改查、分页搜索、低库存预警
3. **入库管理**：创建入库单，自动更新商品库存
4. **出库管理**：创建出库单，自动校验库存并扣减
5. **仪表盘**：首页数据概览（商品总数、库存总量、今日出入库数、低库存商品数、本月入库量）及近七天出入库趋势

### 3.2 非功能需求

1. **安全性**：密码加密存储，接口 Token 鉴权
2. **可靠性**：出入库操作采用数据库事务，保证数据一致性
3. **可维护性**：分层架构（Controller → Service → Mapper），代码结构清晰

---

## 第四章 系统设计

### 4.1 系统架构

本系统采用 B/S 三层架构：前端（Vue.js）通过 HTTP 请求调用后端 RESTful API，后端采用 Controller → Service → Mapper 分层设计，数据层使用 MySQL 数据库。系统架构图如图4.1所示。

（此处插入系统架构图）

### 4.2 功能模块设计

根据需求分析，将系统划分为六个功能模块，系统功能模块图如图4.2所示。

（此处插入功能模块图）

### 4.3 数据库设计

本系统共设计 8 张核心数据表，包括用户表（user）、商品表（product）、库存表（stock）、仓库表（warehouse）、入库单表（inbound）、入库明细表（inbound_detail）、出库单表（outbound）和出库明细表（outbound_item）。各表结构如下：

**表4.1 用户表（user）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 用户ID，主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(100) | 密码，BCrypt加密 |
| role | TINYINT(4) | 角色：1管理员 0普通用户 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

**表4.2 商品表（product）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 商品ID，主键，自增 |
| name | VARCHAR(100) | 商品名称 |
| sku | VARCHAR(100) | 商品编码，唯一 |
| category | VARCHAR(100) | 商品分类 |
| price | DECIMAL(10,2) | 单价 |
| unit | VARCHAR(20) | 单位 |
| low_stock_threshold | INT | 库存预警阈值 |
| status | TINYINT(4) | 状态：0停用 1启用 |
| description | TEXT | 商品描述 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

**表4.3 库存表（stock）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 库存ID，主键，自增 |
| product_id | BIGINT | 商品ID |
| warehouse_id | BIGINT | 仓库ID |
| quantity | INT | 当前库存数量 |
| version | INT | 乐观锁版本号 |
| update_time | DATETIME | 更新时间 |

**表4.4 仓库表（warehouse）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 仓库ID，主键，自增 |
| name | VARCHAR(100) | 仓库名称 |
| location | VARCHAR(200) | 仓库地址 |
| manager | VARCHAR(100) | 负责人 |
| created_at | DATETIME | 创建时间 |

**表4.5 入库单表（inbound）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 入库单ID，主键，自增 |
| inbound_no | VARCHAR(100) | 入库单号，唯一 |
| supplier | VARCHAR(100) | 供应商 |
| total_quantity | INT | 总数量 |
| status | VARCHAR(20) | 状态：PENDING/COMPLETED |
| warehouse_id | BIGINT | 仓库ID |
| operator_id | BIGINT | 操作人ID |
| remark | VARCHAR(500) | 备注 |
| created_at | DATETIME | 创建时间 |

**表4.6 入库明细表（inbound_detail）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 明细ID，主键，自增 |
| inbound_id | BIGINT | 入库单ID |
| product_id | BIGINT | 商品ID |
| quantity | INT | 入库数量 |
| price | DECIMAL(10,2) | 入库单价 |

**表4.7 出库单表（outbound）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 出库单ID，主键，自增 |
| outbound_no | VARCHAR(100) | 出库单号，唯一 |
| customer | VARCHAR(100) | 客户名称 |
| total_quantity | INT | 总数量 |
| status | VARCHAR(20) | 状态：PENDING/COMPLETED |
| created_at | DATETIME | 创建时间 |

**表4.8 出库明细表（outbound_item）**

| 字段名 | 类型 | 说明 |
|:--|:--|:--|
| id | BIGINT | 明细ID，主键，自增 |
| outbound_id | BIGINT | 出库单ID |
| item_id | BIGINT | 商品ID |
| quantity | INT | 出库数量 |

---

## 第五章 系统实现

### 5.1 用户认证模块

用户认证模块实现用户的注册与登录功能。注册时，系统通过 BCryptPasswordEncoder 对用户密码进行加密后存入数据库。登录时，系统校验密码正确性后生成 UUID Token，并将其存入内存 TokenStore 中。后续请求需在 Header 中携带 Authorization 字段，由 LoginInterceptor 拦截器校验 Token 有效性。

登录接口关键代码如下：

```java
String token = UUID.randomUUID().toString();
TOKEN_STORE.put(token, username);

Map<String, Object> data = new HashMap<>();
data.put("username", user.getUsername());
data.put("role", user.getRole());
data.put("token", token);
return Result.success(data);
```

登录拦截器校验 Token 的关键代码如下：

```java
String token = request.getHeader("Authorization");
if (StringUtils.hasText(token) && userService.validateToken(token)) {
    return true;
}
response.setContentType("application/json;charset=utf-8");
response.getWriter().write(new ObjectMapper().writeValueAsString(
    Result.error(401, "请先登录")));
return false;
```

### 5.2 商品管理模块

商品管理模块实现商品的新增、修改、删除、分页查询及低库存预警功能。分页查询支持按商品名称和 SKU 进行模糊搜索，查询结果按创建时间倒序排列。新增商品时对 SKU 进行唯一性校验。低库存预警通过 LEFT JOIN 关联库存表，筛选库存量低于预警阈值的商品。

低库存查询 SQL 如下：

```java
@Select("SELECT p.id, p.name, p.sku, p.category, " +
        "p.low_stock_threshold as lowStockThreshold, " +
        "COALESCE(s.quantity, 0) as stock " +
        "FROM product p " +
        "LEFT JOIN stock s ON p.id = s.product_id " +
        "WHERE p.status = 1 AND p.low_stock_threshold > 0 " +
        "AND COALESCE(s.quantity, 0) <= p.low_stock_threshold " +
        "ORDER BY p.name")
List<Map<String, Object>> selectLowStock();
```

### 5.3 入库管理模块

入库管理模块实现入库单的创建功能。系统接收入库单号、供应商、商品明细等参数后，依次完成单号唯一性校验、商品明细合法性校验，然后写入入库单主表和明细表，最后更新对应商品的库存数量。整个操作在 @Transactional 注解的保护下执行，任何步骤失败均会回滚，保证数据一致性。

库存更新逻辑的关键代码如下：

```java
Stock stock = stockMapper.selectOne(qw);
if (stock == null) {
    stock = new Stock();
    stock.setProductId(item.getProductId());
    stock.setWarehouseId(inbound.getWarehouseId());
    stock.setQuantity(item.getQuantity());
    stockMapper.insert(stock);
} else {
    stock.setQuantity(stock.getQuantity() + item.getQuantity());
    stockMapper.updateById(stock);
}
```

### 5.4 出库管理模块

出库管理模块实现出库单的创建功能。与入库不同，出库需要对库存进行扣减，因此在写入出库单之前需要逐项检查库存是否充足。若任一商品库存不足，系统直接返回错误信息并提示当前剩余数量。库存充足则扣减数量后写入出库单。

库存扣减校验的关键代码如下：

```java
Stock stock = stockMapper.selectOne(qw);
if (stock == null || stock.getQuantity() < item.getQuantity()) {
    return Result.error("商品ID [" + item.getProductId() 
        + "] 库存不足！当前剩余: " 
        + (stock == null ? 0 : stock.getQuantity()));
}
stock.setQuantity(stock.getQuantity() - item.getQuantity());
stockMapper.updateById(stock);
```

### 5.5 仪表盘模块

仪表盘模块为首页提供数据概览和趋势分析功能。统计概览接口（/dashboard/stats）通过聚合查询返回商品总数、总库存量、今日出入库单数、低库存商品数及本月入库总量等关键指标。趋势接口（/dashboard/trend）通过 UNION ALL 生成最近 7 天日期序列，与入库表和出库表的聚合数据进行左连接，确保无数据的天数也返回零值。

趋势查询 SQL 的关键结构如下：

```java
@Select("SELECT DATE_FORMAT(d.date, '%m/%d') as date, " +
        "COALESCE(t.inbound, 0) as inbound, " +
        "COALESCE(o.outbound, 0) as outbound " +
        "FROM ( " +
        "  SELECT CURDATE() - INTERVAL 6 DAY as date " +
        "  UNION ALL SELECT CURDATE() - INTERVAL 5 DAY " +
        "  ... " +
        ") d " +
        "LEFT JOIN (...) t ON d.date = t.dt " +
        "LEFT JOIN (...) o ON d.date = o.dt " +
        "ORDER BY d.date")
```

---

## 第六章 系统测试

### 6.1 测试环境

- 操作系统：Windows 10
- 数据库：MySQL 8.0.37
- JDK：Amazon Corretto 17.0.12
- 接口测试工具：Apifox

### 6.2 功能测试

**表6.1 用户认证测试**

| 测试项 | 输入 | 期望结果 | 实际结果 |
|:--|:--|:--|:--|
| 注册新用户 | username=test, password=123456 | 返回200，注册成功 | 通过 |
| 重复用户名注册 | 已存在的用户名 | 返回500，用户名已存在 | 通过 |
| 正确登录 | 正确的用户名和密码 | 返回200，含token | 通过 |
| 错误密码 | 正确用户名，错误密码 | 返回500，密码错误 | 通过 |
| 无Token请求 | 不带Authorization头 | 返回401，请先登录 | 通过 |

**表6.2 商品管理测试**

| 测试项 | 输入 | 期望结果 | 实际结果 |
|:--|:--|:--|:--|
| 新增商品 | 填写完整商品信息 | 返回200 | 通过 |
| SKU重复 | 已存在的SKU | 返回500，商品编码已存在 | 通过 |
| 分页查询 | pageNum=1, pageSize=10 | 返回分页数据 | 通过 |
| 关键词搜索 | keyword=螺丝 | 返回匹配商品 | 通过 |
| 低库存查询 | 无参数 | 返回预警商品列表 | 通过 |

**表6.3 出入库测试**

| 测试项 | 输入 | 期望结果 | 实际结果 |
|:--|:--|:--|:--|
| 正常入库 | 完整入库信息 | 返回200，库存增加 | 通过 |
| 单号重复 | 已存在的入库单号 | 返回500，单号已存在 | 通过 |
| 正常出库 | 库存充足 | 返回200，库存减少 | 通过 |
| 库存不足出库 | 出库数量>库存 | 返回500，库存不足 | 通过 |

### 6.3 测试结论

经功能性测试，系统各模块运行正常，用户认证、商品管理、出入库操作及仪表盘统计等功能均符合预期设计要求，系统具备良好的可用性和稳定性。

---

## 第七章 总结与展望

### 7.1 总结

本文设计并实现了一款基于 Spring Boot 的仓库管理系统，完成了以下工作：

1. 分析了仓库管理的核心业务流程，梳理了功能与非功能需求
2. 设计了系统数据库结构，包含 8 张核心数据表
3. 实现了用户认证、商品管理、入库管理、出库管理及仪表盘统计五个功能模块
4. 在安全方面，采用 BCrypt 算法加密用户密码，实现了 Token 身份认证拦截机制
5. 出入库操作基于 Spring 声明式事务管理，确保了数据一致性

### 7.2 展望

本系统已在核心功能上满足基本使用需求，但仍存在进一步优化的空间：

1. Token 可引入过期机制与刷新策略，提高系统的安全性
2. 可增加操作日志模块，记录用户的每次操作，便于审计与追溯
3. 可扩展仓库间调拨、盘点等高级功能，满足更复杂的业务场景


---

## 参考文献

[1] 曹晓刚，蒋靖．深入JAVA虚拟机（原书第2版）[M]．北京：机械工业出版社，2015.9:2-8.

[2] [补充书籍或论文]

[3] [补充书籍或论文]

[4] [补充书籍或论文]

[5] [补充书籍或论文]
